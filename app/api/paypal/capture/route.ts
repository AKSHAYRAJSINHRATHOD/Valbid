import { NextResponse } from 'next/server'
import { createClient } from '@supabase/supabase-js'

function base(){return process.env.PAYPAL_ENV==='live'?'https://api-m.paypal.com':'https://api-m.sandbox.paypal.com'}
async function token(){const id=process.env.PAYPAL_CLIENT_ID,secret=process.env.PAYPAL_CLIENT_SECRET;if(!id||!secret)throw new Error('PayPal credentials are not configured.');const r=await fetch(base()+'/v1/oauth2/token',{method:'POST',headers:{Authorization:'Basic '+Buffer.from(id+':'+secret).toString('base64'),'Content-Type':'application/x-www-form-urlencoded'},body:'grant_type=client_credentials',cache:'no-store'});if(!r.ok)throw new Error('PayPal authentication failed.');return (await r.json()).access_token}
export async function GET(req:Request){try{const orderId=new URL(req.url).searchParams.get('orderId');if(!orderId)return NextResponse.json({error:'orderId is required.'},{status:400});const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY,service=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key||!service)return NextResponse.json({error:'Server payment configuration is incomplete.'},{status:503});const authHeader=req.headers.get('authorization');if(!authHeader)return NextResponse.json({error:'Authentication required.'},{status:401});const sb=createClient(url,key,{global:{headers:{Authorization:authHeader}}});const {data:auth}=await sb.auth.getUser();if(!auth.user)return NextResponse.json({error:'Authentication required.'},{status:401});const admin=createClient(url,service);const {data:payment,error}=await admin.from('payments').select('id,amount,currency,status,user_id,provider,provider_payment_id,claim_id').eq('provider_payment_id',orderId).eq('user_id',auth.user.id).eq('provider','paypal').maybeSingle();if(error||!payment)return NextResponse.json({error:'Payment order not found.'},{status:404});if(payment.status==='completed')return NextResponse.json({ok:true,status:'completed'});const t=await token();const r=await fetch(base()+'/v2/checkout/orders/'+encodeURIComponent(orderId)+'/capture',{method:'POST',headers:{Authorization:'Bearer '+t,'Content-Type':'application/json','PayPal-Request-Id':'capture-'+payment.id},body:'{}'});const order=await r.json();if(!r.ok)return NextResponse.json({error:'PayPal capture failed.',details:order},{status:502});const unit=order.purchase_units?.[0],capture=unit?.payments?.captures?.[0],amount=capture?.amount;if(order.status!=='COMPLETED'||capture?.status!=='COMPLETED'||amount?.currency_code!==payment.currency||Number(amount?.value)!==Number(payment.amount))return NextResponse.json({error:'Payment could not be verified.'},{status:409});const {data:processed,error:pe}=await admin.rpc('process_verified_paypal_payment',{p_payment_id:payment.id,p_provider_payment_id:orderId,p_capture_id:capture.id});if(pe)return NextResponse.json({error:'Payment captured but activation failed. The webhook will retry activation.',details:pe.message},{status:500});return NextResponse.json({ok:true,status:'completed',result:processed});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unexpected error.'},{status:500})}}
